schema @sdl(files: [
              "author.graphql",
              "domain.graphql",
              "email.graphql",
              "verifier.graphql",
              "count.graphql",
            ]) {
  query: Query
}
